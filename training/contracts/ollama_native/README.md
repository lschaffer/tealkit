# Ollama Native Contract

This contract is reserved for future training data and notebooks that target Ollama-native tool behavior.

During this first refactor slice, the directory and config scaffolding are created, but dataset generation and evaluation remain to be specialized further.
# ollama_native contract

This contract is the future Ollama-oriented training path for native tool calling.

The current scaffold adds separate config and quality-gate files so the pipeline can evolve without copying the full script tree. The native dataset generation and final native probe contract still need to be implemented on top of this structure.
