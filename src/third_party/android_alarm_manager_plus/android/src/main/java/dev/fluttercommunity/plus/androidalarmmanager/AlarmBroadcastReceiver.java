package dev.fluttercommunity.plus.androidalarmmanager;
// Class moved to AlarmBroadcastReceiver.kt — this file is intentionally empty.
// Joint Kotlin/Java compilation (Flutter 3.44 / AGP 9+) resolves the Kotlin
// class directly, so the old Java shim is no longer needed and caused a
// Redeclaration error when both files were compiled together.
