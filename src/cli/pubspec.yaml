name: tealkit_cli
description: "TealKit native CLI — server management, configuration auto-discovery, task runner, and direct skill prompt execution."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.8.0

dependencies:
  args: ^2.6.0
  yaml: ^3.1.4
  http: ^1.2.2
  path: ^1.9.0
  tealkit_api:
    path: ../api
  mcp_playground_dart:
    path: ../../mcp_playground/mcp_playground_dart

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
